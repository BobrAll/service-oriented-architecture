spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/studs}
    driver-class-name: ${DB_DRIVER:org.postgresql.Driver}
    username: ${DB_USERNAME:s335065}
    password: ${DB_PASSWORD:RnIXdSSUHSXRZDkr}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect